package com.tourismapp.controller.resourceController;

import com.tourismapp.controller.mainController.MainControllerServlet;
import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

import java.io.IOException;
import java.util.List;

@WebServlet(name = "TemplatePageServlet", urlPatterns = {"MainControllerServlet.TEMPLATE_NAME_PAGE_SERVLET"})
public class TemplatePageServlet extends HttpServlet {

//    private final CanvasServiceImpl canvasService = new CanvasServiceImpl();
//    private final CanvasCommentServiceImpl canvasCommentService = new CanvasCommentServiceImpl();
    
    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        String action = req.getParameter("action");
        String id = req.getParameter("id");
        
        if (action == null) {
            action = "";
        }
        
        switch (action) {
//            case MainControllerServlet.ACTION_SEARCH ->
//                    searchByCategory(req, resp);
//            case MainControllerServlet.CANVASPAGE_REDIRECT -> {
//                if (id != null && !id.isEmpty()) {
//                    showDetailCanvas(req, resp);
//                } else {
//                    showListCanvas(req, resp);
//                }
//            }
        }
    }

    @Override
    protected void doPost(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {
        String action = request.getParameter("action");

        if (action == null) {
            action = "";
        }
        
        switch (action) {
//            case MainControllerServlet.ACTION_ADD ->
//                add...(request, response);
//            case MainControllerServlet.ACTION_UPDATE ->
//                update...(request, response);
//            case MainControllerServlet.ACTION_DELETE ->
//                delete...(request, response);
//            default ->
//                show...Detail(request, response);
        }
    }
    
    // Bỏ các function vào  <editor-fold> ở dưới để gọn code func
    // <editor-fold defaultstate="collapsed" desc=" functional ... ">
    
    // </editor-fold>
}

